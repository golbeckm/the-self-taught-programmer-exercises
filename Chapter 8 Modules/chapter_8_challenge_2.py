# Create a module named cubed with a function that takes a number as a
# parameter, and returns the number cubed. Import and call the function
# from another module.

import cubed

num_1 = int(input("What number do you want to cube? "))
print(num_1, "cubed is", cubed.print_cubed(num_1))

